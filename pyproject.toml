[project]
name = "prusa-status"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "paho-mqtt>=2.1.0",
    "printrun",
    "regex>=2024.11.6",
]

[tool.uv.sources]
printrun = { path = "lib/Printrun" }

[tool.pyright]
venvPath = "."
venv = ".venv"
